import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function debug() { console.log("Credentials:", await prisma.credential.findMany({ select: { id: true, application: { select: { applicationType: true } }, certificateGenerated: true } })); console.log("Templates:", await prisma.certificateTemplate.findMany({ select: { id: true, detectedType: true, status: true } })); } debug().finally(() => prisma.$disconnect());
